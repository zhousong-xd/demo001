# vivid/

- `{fox,rabbit,crane}/` — T-067 layered single poses (ACCEPT). See `ASSETS.md` / `catalog.html`.
- Root `*_idle|walk|stand|sit|….png` + `.json` — T-068 **placeholder sprite strips** for true frame-cut playback (not CSS bob). Rebuild: `python3 banquet-pilot/scripts/bake-vivid-sheets.py`.
- High-res true sequence frames: T-069 (replace strips in place).

