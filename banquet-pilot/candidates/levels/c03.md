# C03: 不是所有矛盾都要换座

| Field | Value |
| --- | --- |
| Candidate ID | C03 |
| Working title | 不是所有矛盾都要换座 |
| Status | brief+enum (shipped-ref L03) |
| Formal level | `levels/l03.json` (product freeze; do not edit here) |
| ruleVersion | **0.1** |
| Playable claim | **No** — design brief / reference only |
| Expected solution count | **0** no-calm / **1** calm-on-rabbit (invalid calm targets **0**; enum via candidates tooling) |

## Learning goal

Recognize a **structural** conflict that seat-swaps alone cannot fix; use one `calm_bell` to change a conditional rule (`not_faces_unless`); understand calm **follows the character**, not the chair. Still only the six shipped rule kinds + existing L03 prop pattern — **no new prop kinds**.

## Seats / guests / rules sketch

**Layout:** `6` → seats `A1 A2 A3 B1 B2 B3`.

**Guests:** all six (`fox`, `rabbit`, `crane`, `otter`, `tanuki`, `hedgehog`).  
**Props (existing only):** `calm_bell` stock **1**, valid target **`rabbit` only** (invalid targets explained, no consume).

**Rules (existing six kinds — this brief uses all six kinds + calm gating on one rule):**

| Sketch ID | kind | subject | other / seat / unless | Player-facing intent |
| --- | --- | --- | --- | --- |
| C03-r1 | `at` | fox | seat A1 | 预约锚点 |
| C03-r2 | `at` | crane | seat B2 | 预约锚点 |
| C03-r3 | `faces` | otter | other crane | 隔桌与白先生碰杯 |
| C03-r4 | `end` | hedgehog | — | 端位 |
| C03-r5 | `same_row` | hedgehog | other fox | 与阿狐同排 |
| C03-r6 | `faces` | tanuki | other hedgehog | 面对小刺 |
| C03-r7 | `not_faces_unless` | rabbit | other fox; `unless_state: calm` | 无安心时不能面对阿狐 |

- Intent: without calm on rabbit, full legal seating is designed to be blocked by C03-r7 vs forced faces geometry; with calm, seating can complete. Brief does **not** assert enum counts (**TBD**).
- Calm only exempts this conditional faces ban; does not rewrite seats or other rules.
- No new mechanics (no second prop, no dual tables, no runtime LLM).

## Expected solution count

- **no calm:** `720` perms → **0** solutions  
- **calm on rabbit:** `720` perms → **1** solution  
  `{fox: A1, rabbit: B1, crane: B2, otter: A2, tanuki: B3, hedgehog: A3}`  
- **calm on fox/crane/otter/tanuki/hedgehog:** **0** each (invalid / non-exempting for C03-r7)  

Tooling: `candidates/levels/enumerate_candidates.py` (+ JS twin); product untouched. Calm_bell pattern matches L03 teaching only.

## Tips outline (3 tiers)

1. 这一桌不只有换座这一种办法。
2. 团团的要求带着铃铛图标，表示可以被道具改变。
3. 给团团使用安心铃，再把她安排到阿狐对面。

(Aligned to #4 game-design baseline §11; product hint copy stays frozen with product SHA.)

## Dependency

- **ruleVersion:** `0.1` (kinds: `at` · `not_beside` · `faces` · `end` · `same_row` · `not_faces_unless`; calm_bell as in shipped L03 only).
- Design baseline: #4 game design v0.1 (§7 prop, §11 L03).
- Product freeze reference SHA: `f28893f3c419d794c6ba102bac67038683cb462c`.

## Non-claims / stop lines

- Not a playable level file; must not overwrite formal `levels/l03.json` from this folder.
- Extra prop kinds / new rule kinds are **out of scope**.
- Does not claim playable backlog beyond frozen L01–L03 product.
