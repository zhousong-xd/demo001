# C09: 同一桌的两种解

| Field | Value |
| --- | --- |
| Candidate ID | C09 |
| Working title | 同一桌的两种解 |
| Status | brief (candidates design) |
| Formal level | — (none; not promoted) |
| ruleVersion | **0.1** |
| Playable claim | **No** — design candidate / intent only |
| Expected solution count | **8** / 720 (multiple / symmetric OK) |

## Learning goal

Make **multiple legal solutions** feel intentional: accept symmetry or alternate legal seatings on the same table. Settlement / feedback must treat **all** legal solutions as equivalent — no “one true photo” shame (baseline §13 #09; also §5 镜像解、多个合法解必须接受). Still only the six shipped rule kinds — **no new mechanics**.

## Seats / guests / rules sketch

**Layout:** `6` → seats `A1 A2 A3 B1 B2 B3`.

**Guests:** all six (`fox`, `rabbit`, `crane`, `otter`, `tanuki`, `hedgehog`).  
**Props:** none.

**Rules (existing six kinds only — this brief uses ≤4 kinds; few `at`, favor relational symmetry):**

| Sketch ID | kind | subject | other / seat | Player-facing intent |
| --- | --- | --- | --- | --- |
| C09-r1 | `at` | fox | seat A2 | 唯一中位锚（保留左右/上下对称空间） |
| C09-r2 | `faces` | rabbit | other fox | 团团面对阿狐（中列对面可互换行） |
| C09-r3 | `same_row` | otter | other crane | 阿獭与白先生同排（可落在 A 或 B） |
| C09-r4 | `not_beside` | tanuki | other hedgehog | 狸掌柜与小刺不要紧挨 |
| C09-r5 | `end` | hedgehog | — | 小刺端位（左右端均可，促成对称解） |

- **Multiple legal solutions (intent):** prefer a rule set whose legal set includes at least a **left/right** or **row-swap** symmetry (exact N = TBD at enum). Do **not** add asymmetric `at` anchors that collapse to a unique photo unless a later draft deliberately decides otherwise.
- Settlement intent (not a new mechanic): any SATISFIED full seating clears the level the same way — no star for “canonical” arrangement.
- Contrast with C04 (“自由入席” fewer fixed seats): C09’s teaching beat is explicitly **“两种（或多种）解都对”**, not only “feels like solving.”
- Kinds **not** used here: `not_faces_unless`. No calm_bell.

**Geometry reminders:** ends = index 1 and 3; faces = same index across rows; mirror / multi-OK is a first-class design goal per baseline §5 / §13 #09.

## Expected solution count

Enumerated (T-013, candidates-only): **8**/720. Multiple / symmetric legal arrangements confirmed (row-swap + left/right end pairs). Product untouched.

## Tips outline (3 tiers)

1. 这一桌可以有不止一种坐法。
2. 左右对调或换排，只要关系都满足就算对。
3. 先放好阿狐那一列，再试同排与端位的另一种对称摆法。

(Aligned to #4 game-design baseline §13 #09 同一桌的两种解 — 接受对称或多种合法安排；结算明确所有合法解等价.)

## Dependency

- **ruleVersion:** `0.1` (kinds: `at` · `not_beside` · `faces` · `end` · `same_row` · `not_faces_unless`).
- Design baseline: #4 game design v0.1 §13 (09 同一桌的两种解) + §5 multi-solution acceptance.
- Product freeze reference SHA: `f28893f3c419d794c6ba102bac67038683cb462c`.
- Prior stub title “隔位不算旁边” superseded by baseline naming for this slot.

## Non-claims / stop lines

- Not a playable level file; must not overwrite or create formal `levels/` from this folder.
- No new mechanics; no invented solution count.
- Does not claim playable backlog beyond frozen L01–L03 product.
- Solution count filled by T-013 enum (**8**, multi-OK); still not formal `levels/`.
