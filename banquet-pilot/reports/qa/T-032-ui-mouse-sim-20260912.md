# T-032 候选 playtest UI mouse-sim 独立复核（质检·非真机）

> Issue: https://github.com/zhousong-xd/demo001/issues/38  
> 执行：宴席·质检 · role:BOT · work:qa  
> 作者工程自测（T-031 / #37）≠本环独立验收。  
> ACCEPT 由主导落盘；本文件只给候选 playtest UI mouse-sim 结论。

---

## Versions

| Role | Value |
|------|-------|
| **Engineering tip under test** | `1d7d2860fecfdeb1b866653149a0ef6be85cb9a3` |
| **Product SHA (FIXED)** | `f28893f3c419d794c6ba102bac67038683cb462c` |
| **HTML path** | `banquet-pilot/dist/banquet-pilot.html` |
| **HTML size (LF bytes)** | `73182` |
| **HTML sha256** | `19b8e65bbde285719ee43d74d83fcc60cdb3709ae7cc3ccc80d3f36d45d1bcf5` |
| **LF-only / no CR** | YES |
| **Working-tree blob == product SHA blob** | YES (`c17208faa0548c34cb8cc150a5dc4bce96a61b9c`) |
| **Product modified / rebuilt** | **NO** — verified before & after rerun |
| **Script** | `banquet-pilot/tests/evidence/scripts/t031-playtest-ui-smoke.mjs`（先审后复跑同一脚本） |
| **Script blob** | `9813ccc216aa222ec7a667589a296af8594ed1b9`（worktree == tip） |
| **Branch** | `bot/banquet-pilot` |

---

## 1. Product tree verification

| Check | Result |
|-------|--------|
| Product SHA exists | PASS `f28893f3…` |
| HTML bytes(LF) == 73182 | PASS |
| HTML sha256 == `19b8e65…d1bcf5` | PASS |
| LF-only (no CR) | PASS |
| `git hash-object dist` == blob @ product SHA | PASS |
| `f28893f3..HEAD` changes `dist/` or `src/` | **none** |
| After rerun: dist/src still clean | PASS |

本环测的是 `candidates/playtest` 浏览器 mouse-sim，不是改正式包。锚点匹配，未 BLOCKED。

---

## 2. Environment

| Item | Value |
|------|-------|
| Host | Grok Bot box (Linux) |
| Node | v20.19.2 |
| Chrome | 151.0.7922.169 headless=new（CDP `Input.dispatchMouseEvent`） |
| Command | `cd banquet-pilot && node --experimental-websocket tests/evidence/scripts/t031-playtest-ui-smoke.mjs` |
| Window (UTC) | 2026-09-12T06:44:39Z → 06:45:01Z |
| Exit code | `0` |
| Banner | `[PASS] mouse place / undo / switch c01→c02` |
| 作者证据审读 | PASS（shots 01–04 + summary snap 与脚本断言一致） |
| **真机 / 多指 / 触屏** | **未测**（CDP ≠ 真机；不得 PASS） |

---

## 3. 用例矩阵（PASS / FAIL / BLOCKED / 未测）

独立复跑同一脚本 + 抽检截图/snap；不照搬作者 T-031 报告。

| ID | 用例 | Result | 备注 / 证据 |
|----|------|--------|-------------|
| UI-PLACE | c01 候客 fox 鼠标点选 → A1 | **PASS** | snap `fox=A1` seated=1 historyLen=1；shot `02_c01_placed.png` 狐在 A1、候客区无狐 |
| UI-UNDO | 点「撤销」恢复空桌 | **PASS** | snap seated=0 historyLen=0 assignment 全 null；shot `03_c01_undo.png` 与 boot 一致 |
| UI-SWITCH | 切到 c02 不空白 / URL `?c=c02` | **PASS** | snap fileId=c02 levelId=C02 title=隔桌也算陪伴 0/6；shot `04_c02_switched.png` 六座+六候客。路径=mouse focus select + change Event（headless 原生下拉不可靠；作者已标明） |
| DEV | 真机 / 多指 / 实体触屏 | **未测** | 无实体机；CDP mouse-sim ≠ 真机 |

证据：`banquet-pilot/evidence/t032-ui-mouse-sim/`（NOTES / command / rerun-summary / shots / dist-proof / author-reviewed）。

---

## 4. 缺陷（P0–P3）

| Severity | Findings |
|----------|----------|
| P0 | 无 |
| P1 | 无 |
| P2 | 无 |
| P3 | 无 |

说明（非缺陷）：切关在 headless 不能点开原生 `<select>` 下拉，脚本用 mouse-focus + `change` Event。结果页正确切到 C02，不记 FAIL。真机下拉手感仍属未测。

---

## 5. 已测 / 未测

**已测**

- 冻结产品 dist 锚点（SHA / bytes / sha256 / LF / blob）复跑前后一致
- 先审 T-031 脚本与作者证据，再独立复跑同一脚本（exit 0）
- c01 候客 fox 鼠标放置到 A1
- 鼠标点「撤销」恢复空桌
- 切到 c02：不空白、URL `?c=c02`、六座六候客
- 抽检复跑截图 01–04 与 `__CANDIDATE_PLAYTEST__` snap

**未测（明确）**

- 真机
- 多指 / physical multitouch
- 原生 `<select>` 下拉点选（headless 不可靠；本环与作者同一路径）
- 正式关 / 合 PR / 升正式宣称（本环禁止）

---

## 6. Verdict

最低用例（放置 / 撤销 / 切关）均 **PASS**；真机/多指记 **未测**（不记 PASS）。无 P0–P3。产品 dist 未改。

**VERDICT: PASS**（候选 playtest UI mouse-sim 视角。ACCEPT 由主导落盘。作者自测 ≠ 独立验收。）

---

## 7. 经验一行

候选试玩页用鼠标点放置/撤销/切关，在 headless CDP 下可独立复验且顺；真机触屏仍未测，正式包仍冻结。

---

## Honesty / limits

- CDP `Input.dispatchMouseEvent` ≠ 真机 / 触屏 / 多指。
- 切关 = mouse-focus + 合成 `change`，不是原生下拉点击。
- 未改 `dist/`、`src/`、candidates 玩法；未装新依赖；未 merge；未升正式关。
- 未把作者 T-031 自测当作本环验收依据；作者证据仅作审读对照。

— agent:宴席·质检 role:BOT · task:T-032 · UI MOUSE-SIM · issue:#38

## 8. 推送

- branch: `bot/banquet-pilot`
- tip: *(填 push 后 SHA)*
- 仅 reports/qa + evidence/t032；dist/src 未改
